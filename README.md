# DumaFund frontend

React (Vite) SPA for DumaFund. Dev server proxies `/api` to the Django backend.

```bash
npm install
npm run dev    # http://localhost:5173
```

See the repo root `architecture.txt` and `README` docs for the full stack.
